/**
 * The ONLY public package of the tenant bounded context — the contract every
 * other module codes against. In a Spring Modulith app this is where
 * {@code @NamedInterface("contract")} would go.
 */
package com.example.mockito.saas.tenant.contract;

/**
 * Tenant bounded context. Everything directly in this package is
 * package-private — the only way in from another module is through
 * {@link com.example.mockito.saas.tenant.contract}. In a Spring Modulith app
 * this is where {@code @ApplicationModule(allowedDependencies = ...)} would go.
 */
package com.example.mockito.saas.tenant;
